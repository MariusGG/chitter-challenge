

<!DOCTYPE html>
<html lang="en">
<head>
</head>

<body style='text-align: center'>

  <div class="header">
      <h1>Welcome to Chitter ; )</h1>
  </div>

  <div class="topnav">
        <% if @username %>
      <a href="/sign_out"><%=@username %></a>
        <% else %>
      <a href="/sign_in">Sign In</a>
        <% end %>
    </div>

  <form action="/new" method="post">
    <input type="text" name="peep"   placeholder="Peep it here..." required>
    <input type="submit" value="Peep it!">
  </form>

    <%@peep.each do |peep|%>
      <%if peep.username%>
        <b><%=peep.username%></b>
      <%end%>
      <%=peep.text%>
      <div class="peeptime">
        <%=peep.date%> <%=peep.time%><br><br><br>
      </div>
    <%end%>

    <footer class="footer">
      <div class="footer_text">
        <p>This website was built at <span>Makers Academy</span> by Marius Guei 2018</p>
        <p>Website was built using <span>Ruby, Sinatra, PostgreSQL, Capybara, HTML and CSS</span>.</p>
      </div>
      <a href="/"><img src="makers-logo.png" alt="makers logo"></a>
    </footer>

</body>
</html>
